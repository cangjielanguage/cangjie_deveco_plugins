plugins {
    id 'org.jetbrains.intellij.platform.module'
}

dependencies {
    implementation(project(path: ":quality-common"))
}

sourceSets {
    main {
        java.srcDirs = ['src/main/java']
    }
}
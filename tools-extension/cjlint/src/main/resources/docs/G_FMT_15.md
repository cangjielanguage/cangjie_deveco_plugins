G.FMT.15 避免省略浮点数小数点前的 0

【级别】建议

【描述】

在仓颉语言中，浮点数字面量会包含一个小数点，没有要求小数点之前必须有一个数字。虽然不是一个语法错误，但这种格式的数字不太容易明确看出是在写一个浮点数，因此建议在小数点前面和后面都有一个数字，以明确表明是要创建一个浮点数。

【反例】

```cangjie
const N = .5
const V: Float64 = -.32
```

【正例】

```cangjie
const N = 0.5
const V: Float64 = -0.32
```
